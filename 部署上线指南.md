# AI工具导航站 - 部署上线指南

> 网站已开发完成（300+工具，离线可用）。以下是4种上线方案，按推荐度排序。

---

## 方案一：Vercel 部署（最推荐 ⭐）

**特点**：免费、全球CDN、自动HTTPS、30秒上线、无需服务器

### 步骤

1. **安装 Node.js**（如已安装跳过）
   下载：https://nodejs.org/ （选 LTS 版本）

2. **打开终端**，进入项目目录
   ```bash
   cd D:\claw\Claw\ai-tools-nav
   ```

3. **一键部署**
   ```bash
   npx vercel --prod
   ```

4. **首次使用会要求登录**（浏览器自动打开）
   - 用 GitHub / GitLab / 邮箱 注册登录
   - 授权后回到终端

5. **确认配置**（全部按回车用默认值即可）
   ```
   ? Set up and deploy "~/ai-tools-nav"?  Y
   ? Which scope do you want to deploy to?  (回车)
   ? Link to existing project?  N
   ? What's your project's name?  ai-tools-nav (回车)
   ? In which directory is your code located?  ./ (回车)
   ? Want to modify these settings?  N
   ```

6. **部署完成**，终端会输出一个网址：
   ```
   https://ai-tools-nav-xxx.vercel.app
   ```
   这个网址全球可访问，你的网站正式上线了！

---

## 方案二：Netlify 部署（拖拽式，最简单）

**特点**：无需命令行，网页拖拽即上线

### 步骤

1. 打开 https://app.netlify.com/drop
2. 注册登录（GitHub 或邮箱）
3. 把 `D:\claw\Claw\ai-tools-nav\dist` 整个文件夹拖到网页上
4. 自动部署完成，获得网址 `https://xxx.netlify.app`

---

## 方案三：GitHub Pages 部署

**特点**：适合长期维护，可绑定自定义域名

### 步骤

1. 在 GitHub 新建仓库 `ai-tools-nav`
2. 上传 `dist` 目录所有文件到仓库
3. 仓库 Settings → Pages → Source 选 `main` 分支
4. 等待1分钟，访问 `https://你的用户名.github.io/ai-tools-nav/`

---

## 方案四：CloudStudio 沙箱部署（待服务恢复）

AI 内置的云端部署能力，服务恢复后可自动部署。杨工可随时对我说"重试部署"，我会再次调用。

---

## 💡 后续变现步骤（上线后）

1. **绑定自定义域名**（可选，提升专业感）
   - 在 Vercel/Netlify 项目设置里添加域名
   - 域名可在阿里云/腾讯云购买（约 ¥50/年）

2. **申请 Google AdSense**（主要收入来源）
   - 需要：已上线网站 + 一定流量 + 域名
   - 审核：1-14天
   - 通过后自动展示广告，按点击/展示付费

3. **卖付费收录位**（直接收入）
   - 首页"精选推荐"位：¥200-500/月
   - 分类置顶：¥100-300/月

4. **接入统计**（了解流量）
   - Google Analytics 或百度统计
   - 在 index.html 加一段统计代码即可

---

## 📁 项目文件结构

```
ai-tools-nav/
├── dist/              ← 网站文件（部署这个目录）
│   ├── index.html
│   ├── style.css
│   ├── script.js
│   └── tools-data.js  ← 300+工具数据
├── data/              ← 工具数据源文件
│   ├── tools-part1.json
│   ├── tools-part2.json
│   └── tools-part3.json
└── vercel.json        ← Vercel部署配置
```

---

**推荐立即执行方案一（Vercel）或方案二（Netlify拖拽），5分钟内网站就能全球访问。**
